import React from 'react';

import Icon from '../Icon';

const Stop = props => (
  <Icon a11yTitle='Stop' {...props}>
    <rect width='16' height='16' x='4' y='4' fill='none' stroke='#000' strokeWidth='2' />
  </Icon>
);

export default Stop;

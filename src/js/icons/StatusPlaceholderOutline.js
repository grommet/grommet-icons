import React from 'react';

import Icon from '../Icon';

const StatusPlaceholderOutline = props => (
  <Icon a11yTitle='StatusPlaceholderOutline' {...props}>
    <rect width='20' height='20' x='2' y='2' fill='none' stroke='#000' strokeWidth='2' rx='2' />
  </Icon>
);

export default StatusPlaceholderOutline;
